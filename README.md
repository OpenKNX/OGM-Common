# OpenKNX Common Library

OpenKNX Common is a comprehensive library designed to be used once in every OpenKNX Device firmware, providing core functionality and hardware abstraction.

Includes ETS configuration and own group objects, see
[application description (German only)](doc/Applikationsbeschreibung-Common.md#openknx).

## Table of Contents

- [Overview](#overview)
- [Hardware Support](#hardware-support)
- [Configuration](#configuration)
  - [Core Settings](#core-settings)
  - [LEDs](#leds)
  - [Buttons](#buttons)
  - [Heartbeat Modes](#heartbeat-modes)
- [I2C Support](#i2c-support)
- [GPIO Abstraction Layer](#gpio-abstraction-layer)
- [Release Hooks](#release-hooks)
- [Usage](#usage)

---

## Overview

**Main Functions:**
- Setup and management of the KNX stack
- Setup and execution of OpenKNX Modules
- Flash handling for persistent data storage
- Hardware abstraction (GPIO, I2C, LEDs, Buttons)

**Dependencies:**
Designed for use with header files generated by [OpenKNXproducer](https://github.com/OpenKNX/OpenKNXproducer), which provides necessary defines in `knxprod.h`:
```cpp
MAIN_OpenKnxId
MAIN_ApplicationNumber
MAIN_ApplicationVersion
BASE_StartupDelayBase
ParamBASE_StartupDelayTimeMS
BASE_HeartbeatDelayBase
KoBASE_Heartbeat
ParamBASE_HeartbeatDelayTimeMS
```

---

## Hardware Support

| Platform | Support Level | Notes |
|----------|---------------|-------|
| **RP2040 / RP2350** | Full | Reference platform with dual-core support |
| **ESP32** | Experimental | Limited testing |
| **SAMD21** | Legacy | Obsolete - no new hardware development |

### Hardware Configuration (hardware.h)

#### General HW Settings

| Define | Default | Unit | Function | Mandatory/Optional |
|--------|---------|------|----------|-------------------|
| `SAVE_INTERRUPT_PIN` | - | - | Falling edge on this pin triggers power save actions | optional |
| `KNX_UART_RX_PIN` | - | - | UART receive pin to connect to BCU | mandatory (TP devices) |
| `KNX_UART_TX_PIN` | - | - | UART transmit pin to connect to BCU | mandatory (TP devices) |
| `KNX_UART_NUM` | - | - | Hardware UART number | mandatory (TP devices) |
| `SAVE_POWER_PIN` | - | - | Pin to shut off non-critical power | optional |
| `SAVE_POWER_PIN_POWER_OFF` | - | - | Value to power off (e.g., LOW/HIGH) | mandatory if SAVE_POWER_PIN defined |
| `SAVE_POWER_PIN_POWER_ON` | - | - | Value to power on (e.g., LOW/HIGH) | mandatory if SAVE_POWER_PIN defined |

---

## Configuration

### Core Settings

| Define | Default | Unit | Function |
|--------|---------|------|----------|
| `OPENKNX_RECOVERY_TIME` | 6000 | ms | Hold prog button to erase KNX/OpenKNX data (0 to disable) |
| `OPENKNX_DUALCORE` | - | - | Enable dual-core support (RP2040/RP2350 only) |
| `OPENKNX_WATCHDOG` | - | - | Enable watchdog (disable debugger in production) |
| `OPENKNX_WATCHDOG_MAX_PERIOD` | 16 | s | Watchdog timeout period |
| `OPENKNX_WATCHDOG_AUTOERASE_RESETS` | 5 | - | Erase KNX flash after X watchdog resets |
| `OPENKNX_WATCHDOG_AUTOERASE_TIMEOUT` | 16 | s | Reset counter timeout |
| `OPENKNX_MAX_MODULES` | 9 | - | Maximum number of modules |
| `OPENKNX_WAIT_FOR_SERIAL` | 2000 | ms | Wait for serial connection at startup (0=disabled) |
| `OPENKNX_MAX_LOOPTIME` | 4000 | µs | Soft limit for loop execution time |
| `OPENKNX_LOOPTIME_WARNING` | 7 | ms | Issue warning if loop exceeds this time |
| `OPENKNX_LOOPTIME_WARNING_INTERVAL` | 1000 | ms | Warning interval for console output |

#### Debug & Development

| Define | Default | Function |
|--------|---------|----------|
| `OPENKNX_DEBUG` | - | Enable debug mode |
| `OPENKNX_TRACE1..5` | - | Enable tracing with regex filters |
| `OPENKNX_DEBUGGER` | - | Enable SWD debugger support (disables watchdog) |
| `OPENKNX_RTT` | - | Enable RTT mode (disable USB serial) |
| `BUFFER_SIZE_UP` | 1024 | RTT buffer size in bytes |
| `OPENKNX_HEAP_INTEGRITY_TRACE` | - | ESP32 only: heap-integrity tripwires along the boot restore path — dev only, off in release |

> **`OPENKNX_HEAP_INTEGRITY_TRACE`** walks the whole heap (`heap_caps_check_integrity_all`) at each boot restore step and logs an `OPENKNX_HEAPCHK` marker; the first `*** HEAP CORRUPT ***` bounds the step that corrupted the heap (heap-overrun crashes otherwise surface far away, in `showMemory`'s heap-walk). Zero cost when off.

#### Runtime Statistics

| Define | Default | Unit | Function |
|--------|---------|------|----------|
| `OPENKNX_RUNTIME_STAT` | - | - | Enable runtime statistics collection |
| `OPENKNX_RUNTIME_STAT_BUCKETN` | 16 | - | Number of histogram buckets |
| `OPENKNX_RUNTIME_STAT_BUCKETS` | default | µs | Histogram bucket limits (comma-separated) |

#### File System & Time

| Define | Default | Function |
|--------|---------|----------|
| `OPENKNX_LITTLE_FS` | arch-dep | Enable LittleFS (default: RP2040/ESP32) |
| `OPENKNX_TIME_CLOCK` | arch-dep | Time class implementation |
| `OPENKNX_TIME_DIGAGNOSTIC` | - | Enable time diagnostic commands |
| `OPENKNX_TIME_TESTCOMMAND` | - | Enable time test commands |
| `OPENKNX_OVERRIDE_MASK_VERSION` | - | Override mask version for media type mismatch |

---

### Memory Management

#### PSRAM Helper Macros
On ESP32 and RP2350, you can utilize PSRAM for both static data and dynamic memory allocation. The following macros are provided in `helper.h`:

| Macro | Function | With OPENKNX_PSRAM | Without OPENKNX_PSRAM |
|-------|----------|-------------------|----------------------|
| `PSRAM_MALLOC(size)` | Allocate dynamic memory | ESP32 `ps_malloc`, RP2350 `pmalloc` | `malloc(size)` |
| `PSRAM_CALLOC(count, size)` | Allocate and zero memory | ESP32 `ps_calloc`, RP2350 `pcalloc` | `calloc(count, size)` |
| `PSRAM_REALLOC(ptr, size)` | Reallocate memory | ESP32 `ps_realloc`, RP2350 `realloc` | `realloc(ptr, size)` |
| `psram_new(Type)` | Placement-new in PSRAM | PSRAM allocation | Normal allocation |
| `psram_delete(p)` | Destruct + free PSRAM object | `~T()` + `free()` | `delete p` |
| `PsramAllocator<T>` | STL allocator using PSRAM | `PSRAM_MALLOC`-backed | `std::allocator<T>` |
| `PSRAM_DATA` | Place variable/array in PSRAM | RP2350 section `.psram`, ESP32 `EXT_RAM_BSS_ATTR` | No-op |
| `PSRAM_CODE` | Place function in PSRAM | RP2350 section `.psram_code` | No-op (ESP32 cannot execute from PSRAM) |

The section names are not free-form: arduino-pico's linker script collects `*(.psram*)` into the PSRAM region (`lib/rp2350/memmap_default.ld`), ESP-IDF uses `.ext_ram.bss`. A section name outside those patterns links without error and silently ends up in normal RAM — verify placement in the `.map` file (RP2350 PSRAM window starts at `0x11000000`).

**ESP32 caveat verified by build:** `PSRAM_DATA` only places data in PSRAM if the underlying `sdkconfig` has `CONFIG_SPIRAM_ALLOW_BSS_SEG_EXTERNAL_MEMORY=y` — `EXT_RAM_BSS_ATTR` degrades itself to a no-op otherwise (checked in `esp_attr.h`), and the sdkconfig shipped with this project's pinned pioarduino release does **not** set it. `PSRAM_MALLOC`/`PsramAllocator` are unaffected — confirmed via `nm` that `PsramAllocator<T>::allocate()` calls the real `ps_malloc`, not a stub. RP2350 has no equivalent gate: `PSRAM_DATA` there is proven placed at `0x11000000` with just `RP2350_PSRAM_CS` set.

`PSRAM_DATA` variables live in a `NOLOAD` section: they **cannot be statically initialized** and are not zeroed at startup. Initialize them explicitly at runtime.

**Control defines:**
- `OPENKNX_PSRAM` — automatically defined when ESP32 `BOARD_HAS_PSRAM` or RP2350 `RP2350_PSRAM_CS` is detected. Only these two are checked, because they are what the cores themselves use to bring the PSRAM heap up — defining any other name would enable the macros without an initialized heap. On PlatformIO the board must also declare `upload.psram_length`, otherwise the PSRAM region has zero length
- `OPENKNX_DISABLE_PSRAM` — define in `hardware.h` to disable PSRAM and force fallback to normal RAM/Flash (useful for debugging with Segger, etc.)

**Example usage:**
```cpp
// Dynamic allocation
uint8_t *buf = (uint8_t*)PSRAM_MALLOC(8192);  // PSRAM if available
free(buf);                                    // always free with free()

MyClass *obj = psram_new(MyClass)();          // Placement-new in PSRAM
psram_delete(obj);                            // destructs + frees correctly

// STL container in PSRAM
std::vector<uint8_t, PsramAllocator<uint8_t>> vec;
vec.resize(8192);

// Static allocation
PSRAM_DATA uint8_t largeBuf[8192];            // Array in PSRAM
PSRAM_CODE void heavyComputation() { }        // Function in PSRAM (frees Flash)
```

### LEDs

#### Basic LED Configuration

| Define | Default | Unit | Function |
|--------|---------|------|----------|
| `OPENKNX_NO_BOOT_PULSATING` | - | - | Disable pulsating LED during boot |
| `OPENKNX_LEDEFFECT_PULSE_FREQ` | 1000 | ms | Pulse effect frequency |
| `OPENKNX_LEDEFFECT_BLINK_FREQ` | 1000 | ms | Blink effect frequency |

#### Serial LED (WS2812/NeoPixel)

Only available on ESP32 and RP2040/RP2350:

| Define | Default | Function |
|--------|---------|----------|
| `OPENKNX_SERIALLED_ENABLE` | - | Enable WS2812/NeoPixel LEDs |
| `OPENKNX_SERIALLED_PIN` | - | GPIO for LED strip |
| `OPENKNX_SERIALLED_NUM` | - | Number of LEDs (ESP32: max 24, RP2040: max 8) |

#### Individual LED Configuration

Each LED can be configured for GPIO or Serial LED mode:

| Define | Default | Notes |
|--------|---------|-------|
| `PROG_LED_PIN` | - | GPIO or LED strip index (zero-based) |
| `PROG_LED_PIN_ACTIVE_ON` | - | LOW or HIGH (GPIO only) |
| `PROG_LED_COLOR` | 63,0,0 | RGB color (Serial LED only) - default: 50% red |
| `INFO1_LED_PIN` | - | GPIO or LED strip index |
| `INFO1_LED_PIN_ACTIVE_ON` | - | LOW or HIGH (GPIO only) |
| `INFO1_LED_COLOR` | 0,63,0 | RGB color - default: 50% green |
| `INFO2_LED_PIN` | - | GPIO or LED strip index |
| `INFO2_LED_PIN_ACTIVE_ON` | - | LOW or HIGH (GPIO only) |
| `INFO2_LED_COLOR` | 0,63,0 | RGB color - default: 50% green |
| `INFO3_LED_PIN` | - | GPIO or LED strip index |
| `INFO3_LED_PIN_ACTIVE_ON` | - | LOW or HIGH (GPIO only) |
| `INFO3_LED_COLOR` | 0,63,0 | RGB color - default: 50% green |

#### Time LED

| Define | Default | Function |
|--------|---------|----------|
| `OPENKNX_LED_TIME` | - | Use LED for time status (values: info1Led, info2Led, info3Led) |

**Time LED States:**

| State | Standard LED | RGB LED | Note |
|-------|--------------|---------|------|
| No valid time | Off | Red | |
| Time updated | Fast flash | Green | For 1 second |
| Time valid | Slow flash | Blue flash | Synced with seconds |

---

### Buttons

| Define | Default | Function |
|--------|---------|----------|
| `PROG_BUTTON_PIN` | - | GPIO for programming button |
| `PROG_BUTTON_PIN_MODE` | INPUT_PULLUP | Pin mode (INPUT_PULLUP, INPUT_PULLDOWN, INPUT) |
| `OPENKNX_BUTTON_DEBOUNCE` | 50 | Software debounce time in ms (0=disabled) |

**Button Features:**
- Short press (<1000ms)
- Long press (>1000ms)
- Double press (500ms intervals)

---

### Heartbeat Modes

Enable heartbeat monitoring to detect hung loops:

| Define | Default | Unit | Function |
|--------|---------|------|----------|
| `OPENKNX_HEARTBEAT` | 1000 | ms | Enable normal heartbeat mode |
| `OPENKNX_HEARTBEAT_PRIO` | 3000 | ms | Enable priority heartbeat mode |
| `OPENKNX_HEARTBEAT_FREQ` | 200 | ms | Heartbeat blink frequency |
| `OPENKNX_HEARTBEAT_PRIO_ON_FREQ` | 200 | ms | Priority mode ON frequency |
| `OPENKNX_HEARTBEAT_PRIO_OFF_FREQ` | 1000 | ms | Priority mode OFF frequency |

#### Normal Mode
- `progLed` blinks when loop is running
- `infoLed` blinks when loop1 is running
- LED stops if loop hangs

#### Priority Mode
- LEDs blink continuously when healthy
- LEDs stop blinking if loop hangs
- `progLed` blinks faster in programming mode
- **If device is NOT blinking, something is wrong!**

---

## I2C Support

OpenKNX Common provides flexible I2C support with both **PIO-based** and **Hardware I2C**.

### Quick Overview

| Bus Type | Count | Names | Features |
|----------|-------|-------|----------|
| **PIO I2C** | 2 | WIRE_PIO, WIRE1_PIO | Auto PIO/SM assignment, flexible pins |
| **Hardware I2C** | 2 | WIRE, WIRE1 | Fixed hardware, standard Wire API |

### Configuration

**For PIO I2C buses (flexible pin assignment):**
```cpp
// PIO I2C Bus 1
#define OPENKNX_WIRE_PIO
#define OPENKNX_WIRE_PIO_SDA_PIN 26
#define OPENKNX_WIRE_PIO_SCL_PIN 27  // Must be SDA + 1!
#define OPENKNX_WIRE_PIO_CLOCK   400000

// PIO I2C Bus 2
#define OPENKNX_WIRE1_PIO
#define OPENKNX_WIRE1_PIO_SDA_PIN 4
#define OPENKNX_WIRE1_PIO_SCL_PIN 5  // Must be SDA + 1!
#define OPENKNX_WIRE1_PIO_CLOCK   100000
```

**For Hardware I2C buses:**
```cpp
// Hardware I2C Bus 1
#define OPENKNX_WIRE
#define OPENKNX_WIRE_SDA_PIN 20
#define OPENKNX_WIRE_SCL_PIN 21
#define OPENKNX_WIRE_CLOCK   400000

// Hardware I2C Bus 2
#define OPENKNX_WIRE1
#define OPENKNX_WIRE1_SDA_PIN 6
#define OPENKNX_WIRE1_SCL_PIN 7
#define OPENKNX_WIRE1_CLOCK   100000
```

### Usage Example
```cpp
// All buses use standard Wire API
WIRE_PIO.beginTransmission(0x3C);
WIRE_PIO.write(data);
WIRE_PIO.endTransmission();

WIRE.beginTransmission(0x48);
WIRE.write(sensorData);
WIRE.endTransmission();
```

### Detailed Documentation

For complete PIO I2C documentation including:
- Automatic PIO/SM allocation
- Pin requirements and constraints
- Performance benchmarks
- Troubleshooting guide
- Advanced features

**See:** [PIO I2C Documentation](src/OpenKNX/I2C/doc/README.md)

---

## GPIO Abstraction Layer

OpenKNX Common includes an abstraction layer for seamless GPIO access across MCU pins and I2C port expanders.

### Configuration (hardware.h)
```cpp
#define OPENKNX_GPIO_NUM 1                     // Number of expanders
#define OPENKNX_GPIO_TYPES OPENKNX_GPIO_T_TCA9555  // Expander type(s)
#define OPENKNX_GPIO_ADDRS 0x20                // I2C address(es)
#define OPENKNX_GPIO_INTS 0xFF                 // Interrupt pins

// I2C Configuration for expanders
#define OPENKNX_GPIO_WIRE Wire
#define OPENKNX_GPIO_CLOCK 400000
#define OPENKNX_GPIO_SDA 28
#define OPENKNX_GPIO_SCL 29

// Example pin mapping
#define OPENKNX_xxx_PINS 0x010D, 0x010B, 0x0102, 0x0104
```

**Important:** If `OPENKNX_GPIO_NUM` ≥ 1, the lists `OPENKNX_GPIO_TYPES`, `OPENKNX_GPIO_ADDRS`, and `OPENKNX_GPIO_INTS` must contain exactly the specified number of entries.

### GPIO Numbering

- **MCU GPIOs:** Use plain pin number (e.g., `24`)
- **Expander 1 GPIOs:** `0x0100` to `0x01FF` (where XX = IO number)
- **Expander 2 GPIOs:** `0x0200` to `0x02FF`
- **Expander N GPIOs:** `0x0N00` to `0x0NFF`

**Examples:**
- `0x0105` = IO 5 on expander 1
- `0x0202` = IO 2 on expander 2
- `24` = MCU GPIO 24

### Usage Example
```cpp
// MCU GPIO
openknx.gpio.pinMode(24, OUTPUT);
openknx.gpio.digitalWrite(24, HIGH);

// Expander GPIO
openknx.gpio.pinMode(0x0105, OUTPUT);        // IO 5 on expander 1
openknx.gpio.digitalWrite(0x0105, HIGH);     // Write HIGH
int value = openknx.gpio.digitalRead(0x0105); // Read state

// Input with pull-up
openknx.gpio.pinMode(0x0202, INPUT_PULLUP);  // IO 2 on expander 2
```

### Supported Hardware

| Chip | Type | I/Os | I2C Address Range |
|------|------|------|-------------------|
| **TCA6408** | 8-bit | 8 | 0x20-0x21 |
| **TCA9555** | 16-bit | 16 | 0x20-0x27 |
| **PCA9554** | 8-bit | 8 | 0x20-0x27 |
| **PCA9557** | 8-bit | 8 | 0x18-0x1F |

### Current Features
- `pinMode(pin, mode)`
- `digitalRead(pin)`
- `digitalWrite(pin, value)`
- `attachInterrupt(pin, callback, mode)`

### Planned Features
- Improved interrupt handling on expanders
- `analogRead/Write` support
- Bulk pin operations
- Multi-core support
- Multiple I2C buses for expanders

---

## Usage

Include OpenKNX Common in your project:
```cpp
#include "OpenKNX.h"

void setup() {
    openknx.init();
}

void loop() {
    openknx.loop();
}
```

### Module Integration
```cpp
class MyModule : public OpenKNX::Module {
    void setup() override {
        // Your module setup
    }
    
    void loop() override {
        // Your module loop
    }
};
```

---

## License

OpenKNX Common Library is part of the OpenKNX project.  
Licensed under GNU GPL v3.0

---

## Release Hooks

`Build-Release-Preprocess.ps1` / `Build-Release-Postprocess.ps1` run a release step for every module
that brings one. The scripts here know no module names -- the hooks are found by convention:

```
lib/<Module>/scripts/release/Pre.ps1     # before the firmware build
lib/<Module>/scripts/release/Post.ps1    # after it, before the release is zipped
```

A module that is not in `lib/` has no hook and contributes nothing, so a product never has to ask
whether it uses a given module.

**Contract**

| | |
|---|---|
| Invocation | `-ReleaseRoot <absolute path of release/>`, `-BuildParam Dev\|Release` |
| Working directory | the project root |
| Order | alphabetical by path, so a release stays reproducible |
| Failure | logged as a warning, the release continues |

The last point is deliberate: hooks ship optional companion artifacts -- a PC tool with its own
release cycle, an example tree -- and a firmware release must never hinge on one.

**Example** -- `OFM-FileTransferModule/scripts/release/Post.ps1` puts the PC FileTransferClient into
the release as `Tools/ftc-cli/<OS>/<arch>/ftc[.exe]`. The layout lives in that module because it is
that module's artifact.

---

## Links

- [OpenKNX Project](https://github.com/OpenKNX)
- [OpenKNXproducer](https://github.com/OpenKNX/OpenKNXproducer)
- [PIO I2C Documentation](src/OpenKNX/I2C/README.md)
